<Window x:Class="Tienda_Virtual.Detalle1"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Tienda_Virtual"
        mc:Ignorable="d"
        Title="Detalle1" 
        Height="400" 
        Width="600"    
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent">
    <Window.Resources>
        <!--#region Botones redondos-->
        <Style x:Key="RoundedButtonStyle" TargetType="Button">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                     BorderBrush="{TemplateBinding BorderBrush}" 
                     BorderThickness="1"
                     CornerRadius="10" Padding="10">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <!--#endregion-->
        <!--#region focusvisual-->
        <Style x:Key="FocusVisual">
            <Setter Property="Control.Template">
                <Setter.Value>
                    <ControlTemplate>
                        <Rectangle Margin="2" StrokeDashArray="1 2" Stroke="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}" SnapsToDevicePixels="true" StrokeThickness="1"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!--#endregion-->
        <!--#region BTNHEADER-->
        <SolidColorBrush x:Key="Button.Static.Background" Color="#FFDDDDDD"/>
        <SolidColorBrush x:Key="Button.Static.Border" Color="#FF707070"/>
        <SolidColorBrush x:Key="Button.MouseOver.Background" Color="#FFBEE6FD"/>
        <SolidColorBrush x:Key="Button.MouseOver.Border" Color="#FF3C7FB1"/>
        <SolidColorBrush x:Key="Button.Pressed.Background" Color="#FFC4E5F6"/>
        <SolidColorBrush x:Key="Button.Pressed.Border" Color="#FF2C628B"/>
        <SolidColorBrush x:Key="Button.Disabled.Background" Color="#FFF4F4F4"/>
        <SolidColorBrush x:Key="Button.Disabled.Border" Color="#FFADB2B5"/>
        <SolidColorBrush x:Key="Button.Disabled.Foreground" Color="#FF838383"/>
        <Style x:Key="BTNHEADERSTYLE" TargetType="{x:Type Button}">
            <Setter Property="FocusVisualStyle" Value="{StaticResource FocusVisual}"/>
            <Setter Property="Background" Value="{StaticResource Button.Static.Background}"/>
            <Setter Property="BorderBrush" Value="{StaticResource Button.Static.Border}"/>
            <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Padding" Value="1"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border x:Name="border" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" SnapsToDevicePixels="true" Cursor="Hand">
                            <ContentPresenter x:Name="contentPresenter" Focusable="False" HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" Margin="{TemplateBinding Padding}" RecognizesAccessKey="True" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsDefaulted" Value="true">
                                <Setter Property="BorderBrush" TargetName="border" Value="{DynamicResource {x:Static SystemColors.HighlightBrushKey}}"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="true">
                                <Setter Property="Background" TargetName="border" Value="#001F3F"/>
                                <Setter Property="BorderBrush" TargetName="border" Value="#001F3F"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="true">
                                <Setter Property="Background" TargetName="border" Value="Transparent"/>
                                <Setter Property="BorderBrush" TargetName="border" Value="Transparent"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="false">
                                <Setter Property="Background" TargetName="border" Value="{StaticResource Button.Disabled.Background}"/>
                                <Setter Property="BorderBrush" TargetName="border" Value="{StaticResource Button.Disabled.Border}"/>
                                <Setter Property="TextElement.Foreground" TargetName="contentPresenter" Value="{StaticResource Button.Disabled.Foreground}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="FocusVisual1">
            <Setter Property="Control.Template">
                <Setter.Value>
                    <ControlTemplate>
                        <Rectangle Margin="2" StrokeDashArray="1 2" Stroke="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}" SnapsToDevicePixels="true" StrokeThickness="1"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <SolidColorBrush x:Key="Button.Static.Background1" Color="#FFDDDDDD"/>
        <SolidColorBrush x:Key="Button.Static.Border1" Color="#FF707070"/>
        <SolidColorBrush x:Key="Button.MouseOver.Background1" Color="#FFBEE6FD"/>
        <SolidColorBrush x:Key="Button.MouseOver.Border1" Color="#FF3C7FB1"/>
        <SolidColorBrush x:Key="Button.Pressed.Background1" Color="#FFC4E5F6"/>
        <SolidColorBrush x:Key="Button.Pressed.Border1" Color="#FF2C628B"/>
        <SolidColorBrush x:Key="Button.Disabled.Background1" Color="#FFF4F4F4"/>
        <SolidColorBrush x:Key="Button.Disabled.Border1" Color="#FFADB2B5"/>
        <SolidColorBrush x:Key="Button.Disabled.Foreground1" Color="#FF838383"/>
        <!--#endregion-->
    </Window.Resources>

    <Border CornerRadius="10"
    Background="AliceBlue">
        <Grid Name="Principal"
      Background="Transparent">
            
            <!--#region  HEADER-->
            <Grid Name="Header" 
          Height="50"
          VerticalAlignment="Top"
          Background="Transparent">

                <Border CornerRadius="10,10,0,0"
                Background="#001F3F"/>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition/>
                        <ColumnDefinition Width="220"/>
                        <ColumnDefinition Width="100"/>
                    </Grid.ColumnDefinitions>
                </Grid>


                <!--#Region TITLE-->
                <Grid>
                    <!-- Forma Rectangular -->
                    <Rectangle Fill="#3A6D8C"
                           Width="150"
                           Height="22"
                           RadiusX="10"  
                           RadiusY="10"/>

                    <!-- Texto dentro de la forma -->
                    <TextBlock Text="Detalles"
                           Foreground="Black"
                           FontSize="15"
                           FontFamily="Comic Sans MS"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"
                           Margin="0,0,0,0"/>
                </Grid>
                <!--#endregion-->


                <!--#region Minimizar-->

                <Button Style="{DynamicResource BTNHEADERSTYLE}" x:Name="BtnMinimizar"
                    Background="Transparent"
                    BorderBrush="Transparent"
                    Height="24"
                    Width="26"
                    HorizontalAlignment="Left"
                    VerticalAlignment="Center"
                    Margin="540,0,0,0" Click="Minimizar">

                    <Image HorizontalAlignment="Left"
                       VerticalAlignment="Center"
                       Width="15"
                       Source="SCS/IMG/Minimizar.png"
                       Cursor="Hand"/>


                </Button>
                <!--#endregion-->

                <!--#region Cerrar-->

                <Button Style="{DynamicResource BTNHEADERSTYLE}" x:Name="BtnCerrar"
                    Background="Transparent"
                    BorderBrush="Transparent"
                    Height="24"
                    Width="26"
                    HorizontalAlignment="Left"
                    VerticalAlignment="Center"
                    Margin="560,0,0,0" Click="Cerrar">

                    <Image HorizontalAlignment="Left"
                       VerticalAlignment="Top"
                       Width="40"
                       Source="SCS/IMG/Cerrar.png"
                       Cursor="Hand"/>


                </Button>

                <!--#endregion-->

                <!--#region Regreso-->
                <Button Style="{DynamicResource BTNHEADERSTYLE}" x:Name="BtnRegresar"
                Background="Transparent"
                BorderBrush="Transparent"
                Height="40"
                Width="40"
                HorizontalAlignment="Left"
                VerticalAlignment="Top"
                Margin="10,5,0,0" 
                Click="Regresar_Click">


                    <Border Background="#79D7BE"  
                        BorderBrush="Transparent"
                        BorderThickness="0"
                        CornerRadius="30"
                        Width="30"
                        Height="30"
                        HorizontalAlignment="Left"
                        VerticalAlignment="Center">

                        <Image HorizontalAlignment="Left"
                            VerticalAlignment="Center"
                            Width="20"
                            Source="SCS/IMG/Regreso.png" 
                            Margin="5,0,0,0" />

                    </Border>
                </Button>
                <!--#endregion-->






            </Grid>
            <!--#endregion-->

            <!--#region Contenido pag-->
            <!-- CONTENIDO PRINCIPAL -->
            <Grid Margin="20,60,20,20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <!-- Imagen -->
                    <ColumnDefinition Width="*" />
                    <!-- Descripción -->
                </Grid.ColumnDefinitions>

                <!-- Imagen del Producto -->
                <Border Grid.Column="0" Width="180" Height="150" BorderThickness="2" BorderBrush="Gray" CornerRadius="10" Margin="0,0,20,0">
                    <Image Source="SCS/IMG/Compu.jpeg" Stretch="Fill" x:Name="imagen"/>
                </Border>

                <!-- Descripción del Producto -->
                <StackPanel Grid.Column="1" VerticalAlignment="Top">
                    <TextBlock x:Name="NombreProductoLabel" Text="Nombre del Producto" 
                               FontSize="20" FontWeight="Bold" Margin="0,50,0,5"/>
                    <TextBlock x:Name="lblDescripcion" Text="Descripción breve del producto. Explica sus características y beneficios." TextWrapping="Wrap" FontSize="14"/>
                    <TextBlock x:Name="lblPrecio" Text="Precio: $" FontSize="16" FontWeight="Bold" Foreground="Green" Margin="0,10,0,0"/>
                </StackPanel>
            </Grid>

            <!-- BOTÓN REDONDO AÑADIR AL CARRITO -->
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,250,0,0">
                <Button x:Name="btnAgregarCarrito" Content="Añadir al carrito"
                        Width="150" Height="40"
                        FontSize="15"
                        Background="#001F3F"  Foreground="White"
                        Style="{StaticResource RoundedButtonStyle}"
                        Margin="20,40,10,0" Click="Button_Click"/>

            </StackPanel>
            <!--#endregion-->
        </Grid>
    </Border>
</Window>
